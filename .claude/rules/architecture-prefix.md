---
description: Prefix for each unit
---

# Prefix

Every unit supports all existing prefix units, both for exponents > 1 and for exponents < 1.

All prefix units and base units must be combinable with one another.

If the user makes an explicit request to exclude prefix units, this must also be implemented for the
corresponding units.
