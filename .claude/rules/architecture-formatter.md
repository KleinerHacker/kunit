---
description: Formatter to format an unit
---

# Architecture

A formatter is a class to format a unit. This contains the calculated number and its unit symbols like `12 km/h`.

## Details

There MUST exist a base for all formatter implementations. It must be extendable by framework users. 