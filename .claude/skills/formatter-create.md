---
name: create-formatter
description: Create new formatter implementation
---

# Architecture

The formatter MUST implements `org.pcsoft.framework.kunit.formatter.KUnitFormatter` interface. The formatter implementation
MUST be placed in the package `org.pcsoft.framework.kunit.formatter`. Check existing implementations. 